# Student Behaviour Detection

This project uses a model trained using Teachable Machine.

The model classifies images to detect:
- Reading a book
- Using a phone

The model is loaded using TensorFlow.js in a simple web interface.
